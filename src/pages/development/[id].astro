---
import { loadDevelopment } from '@/application';

export async function getStaticPaths() {
	const developments = await loadDevelopment();
	return developments.map((development) => ({ params: { id: development.id } }));
}

return Astro.redirect(`/ja/development/${Astro.params.id}`, 301);
---
