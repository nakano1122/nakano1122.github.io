---
import { loadResearch } from '@/application';

export async function getStaticPaths() {
	const researches = await loadResearch();
	return researches.map((research) => ({ params: { id: research.id } }));
}

return Astro.redirect(`/ja/research/${Astro.params.id}`, 301);
---
