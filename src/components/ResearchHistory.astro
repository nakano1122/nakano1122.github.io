---
interface Publication {
	title: string;
	conference: string;
	date: string;
	type: 'journal' | 'conference' | 'report';
	status: 'published' | 'accepted' | 'submitted';
	description?: string;
	authors?: string[];
}

const publications: Publication[] = [
	{
		title: "深層学習を用いた自然言語処理における感情分析の高精度化",
		conference: "情報処理学会第85回全国大会",
		date: "2023年3月",
		type: "conference",
		status: "published",
		description: "BERTベースのモデルに感情特徴抽出レイヤーを追加し、従来手法より15%の精度向上を実現",
		authors: ["中野太郎", "山田花子", "佐藤次郎"]
	},
	{
		title: "グラフニューラルネットワークによるソーシャルネットワーク分析",
		conference: "人工知能学会全国大会",
		date: "2023年6月",
		type: "conference",
		status: "published",
		description: "GCNを用いてソーシャルネットワークの影響力予測を行い、既存手法を20%上回る性能を達成",
		authors: ["中野太郎", "田中三郎"]
	},
	{
		title: "機械学習を活用したリアルタイム異常検知システムの提案",
		conference: "電子情報通信学会信学技報",
		date: "2024年1月",
		type: "report",
		status: "published",
		description: "時系列データに対するTransformerベースの異常検知モデルを提案し、実用的な性能を実証",
		authors: ["中野太郎", "研究室指導教員"]
	}
];

const getTypeColor = (type: string) => {
	switch (type) {
		case 'journal': return 'var(--color-primary)';
		case 'conference': return 'var(--color-secondary)';
		case 'report': return 'var(--color-accent)';
		default: return 'var(--color-text-light)';
	}
};

const getTypeLabel = (type: string) => {
	switch (type) {
		case 'journal': return '学術誌';
		case 'conference': return '学会発表';
		case 'report': return '技術報告';
		default: return type;
	}
};

const getStatusIcon = (status: string) => {
	switch (status) {
		case 'published': return '✅';
		case 'accepted': return '⏳';
		case 'submitted': return '📝';
		default: return '📄';
	}
};
---

<section class="research-history section">
	<div class="container">
		<h2 class="section-title">Research History</h2>
		
		<div class="research-intro animate-slide-up">
			<div class="intro-card">
				<div class="intro-icon">🔬</div>
				<div class="intro-content">
					<h3 class="intro-title">研究分野</h3>
					<p class="intro-description">
						機械学習・深層学習を中心とした人工知能技術の研究に取り組んでいます。
						特に自然言語処理、グラフニューラルネットワーク、異常検知システムの分野で成果を上げています。
					</p>
				</div>
			</div>
		</div>

		<div class="publications-section">
			<h3 class="subsection-title">
				<span class="icon-wrapper">📚</span>
				発表論文・研究報告
			</h3>
			
			<div class="publications-timeline">
				{publications.map((publication, index) => (
					<div class="publication-item animate-slide-up" style={`animation-delay: ${index * 0.15}s`}>
						<div class="timeline-connector">
							<div class="timeline-dot" style={`background-color: ${getTypeColor(publication.type)}`}></div>
							{index < publications.length - 1 && <div class="timeline-line"></div>}
						</div>
						
						<div class="publication-card">
							<div class="publication-header">
								<div class="publication-meta">
									<span class="publication-type" style={`background-color: ${getTypeColor(publication.type)}20; color: ${getTypeColor(publication.type)}`}>
										{getTypeLabel(publication.type)}
									</span>
									<span class="publication-status">
										{getStatusIcon(publication.status)}
									</span>
									<span class="publication-date">{publication.date}</span>
								</div>
							</div>
							
							<div class="publication-content">
								<h4 class="publication-title">{publication.title}</h4>
								<p class="publication-conference">{publication.conference}</p>
								
								{publication.description && (
									<p class="publication-description">{publication.description}</p>
								)}
								
								{publication.authors && (
									<div class="publication-authors">
										<span class="authors-label">著者:</span>
										<div class="authors-list">
											{publication.authors.map((author, authorIndex) => (
												<span class={`author ${author === '中野太郎' ? 'author-self' : ''}`}>
													{author}
													{authorIndex < publication.authors.length - 1 && ', '}
												</span>
											))}
										</div>
									</div>
								)}
							</div>
						</div>
					</div>
				))}
			</div>
		</div>

		<div class="research-stats animate-slide-up">
			<div class="stats-grid">
				<div class="stat-item">
					<div class="stat-number">3</div>
					<div class="stat-label">発表論文数</div>
				</div>
				<div class="stat-item">
					<div class="stat-number">2</div>
					<div class="stat-label">学会発表</div>
				</div>
				<div class="stat-item">
					<div class="stat-number">1</div>
					<div class="stat-label">技術報告</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.research-history {
		background: linear-gradient(135deg, var(--color-bg) 0%, var(--color-bg-secondary) 100%);
	}

	.research-intro {
		margin-bottom: var(--spacing-3xl);
	}

	.intro-card {
		background: var(--color-bg-card);
		border-radius: var(--radius-xl);
		padding: var(--spacing-2xl);
		box-shadow: var(--shadow-md);
		border: 1px solid var(--color-border);
		display: flex;
		align-items: center;
		gap: var(--spacing-xl);
		max-width: 800px;
		margin: 0 auto;
	}

	.intro-icon {
		font-size: 3rem;
		padding: var(--spacing-lg);
		background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
		border-radius: var(--radius-lg);
		display: flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
		box-shadow: var(--shadow-lg);
	}

	.intro-content {
		flex: 1;
	}

	.intro-title {
		font-size: 1.5rem;
		font-weight: 600;
		margin-bottom: var(--spacing-sm);
		color: var(--color-text);
	}

	.intro-description {
		color: var(--color-text-light);
		line-height: 1.7;
	}

	.subsection-title {
		display: flex;
		align-items: center;
		gap: var(--spacing-md);
		font-size: 1.75rem;
		font-weight: 600;
		margin-bottom: var(--spacing-xl);
		color: var(--color-text);
	}

	.icon-wrapper {
		font-size: 2rem;
		padding: var(--spacing-sm);
		background: var(--color-bg-secondary);
		border-radius: var(--radius-md);
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.publications-timeline {
		position: relative;
		max-width: 900px;
		margin: 0 auto;
	}

	.publication-item {
		display: flex;
		margin-bottom: var(--spacing-2xl);
		animation: slideUp 0.6s ease-out both;
	}

	.publication-item:last-child {
		margin-bottom: 0;
	}

	.timeline-connector {
		display: flex;
		flex-direction: column;
		align-items: center;
		margin-right: var(--spacing-xl);
		flex-shrink: 0;
	}

	.timeline-dot {
		width: 16px;
		height: 16px;
		border-radius: 50%;
		border: 4px solid var(--color-bg-card);
		box-shadow: var(--shadow-md);
		z-index: 2;
	}

	.timeline-line {
		width: 2px;
		height: 100%;
		background: var(--color-border);
		margin-top: var(--spacing-sm);
		min-height: 60px;
	}

	.publication-card {
		background: var(--color-bg-card);
		border-radius: var(--radius-lg);
		padding: var(--spacing-xl);
		box-shadow: var(--shadow-md);
		border: 1px solid var(--color-border);
		flex: 1;
		transition: transform 0.3s ease, box-shadow 0.3s ease;
		position: relative;
		overflow: hidden;
	}

	.publication-card:hover {
		transform: translateY(-2px);
		box-shadow: var(--shadow-lg);
	}

	.publication-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 3px;
		background: linear-gradient(90deg, var(--color-primary) 0%, var(--color-secondary) 100%);
	}

	.publication-header {
		margin-bottom: var(--spacing-lg);
	}

	.publication-meta {
		display: flex;
		align-items: center;
		gap: var(--spacing-sm);
		flex-wrap: wrap;
	}

	.publication-type {
		padding: var(--spacing-xs) var(--spacing-sm);
		border-radius: var(--radius-sm);
		font-size: 0.75rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.5px;
	}

	.publication-status {
		font-size: 1.2rem;
	}

	.publication-date {
		color: var(--color-text-lighter);
		font-size: 0.875rem;
		font-weight: 500;
	}

	.publication-title {
		font-size: 1.25rem;
		font-weight: 600;
		margin-bottom: var(--spacing-sm);
		color: var(--color-text);
		line-height: 1.4;
	}

	.publication-conference {
		color: var(--color-text-light);
		font-weight: 500;
		margin-bottom: var(--spacing-md);
		font-style: italic;
	}

	.publication-description {
		color: var(--color-text-light);
		line-height: 1.6;
		margin-bottom: var(--spacing-md);
		padding: var(--spacing-md);
		background: var(--color-bg-secondary);
		border-radius: var(--radius-md);
		border-left: 4px solid var(--color-primary);
	}

	.publication-authors {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: var(--spacing-xs);
	}

	.authors-label {
		font-size: 0.875rem;
		font-weight: 600;
		color: var(--color-text-lighter);
		margin-right: var(--spacing-xs);
	}

	.authors-list {
		display: flex;
		flex-wrap: wrap;
		gap: var(--spacing-xs);
	}

	.author {
		font-size: 0.875rem;
		color: var(--color-text-light);
	}

	.author-self {
		color: var(--color-primary);
		font-weight: 600;
	}

	.research-stats {
		margin-top: var(--spacing-3xl);
	}

	.stats-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
		gap: var(--spacing-lg);
		max-width: 600px;
		margin: 0 auto;
	}

	.stat-item {
		background: var(--color-bg-card);
		border-radius: var(--radius-lg);
		padding: var(--spacing-xl);
		text-align: center;
		box-shadow: var(--shadow-md);
		border: 1px solid var(--color-border);
		transition: transform 0.3s ease;
	}

	.stat-item:hover {
		transform: translateY(-2px);
	}

	.stat-number {
		font-size: 2.5rem;
		font-weight: 700;
		background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		margin-bottom: var(--spacing-xs);
	}

	.stat-label {
		color: var(--color-text-light);
		font-weight: 500;
		font-size: 0.875rem;
	}

	@media (max-width: 768px) {
		.intro-card {
			flex-direction: column;
			text-align: center;
			padding: var(--spacing-xl);
		}

		.intro-icon {
			font-size: 2.5rem;
			padding: var(--spacing-md);
		}

		.timeline-connector {
			margin-right: var(--spacing-md);
		}

		.publication-card {
			padding: var(--spacing-lg);
		}

		.publication-meta {
			flex-direction: column;
			align-items: flex-start;
		}

		.publication-title {
			font-size: 1.1rem;
		}

		.stats-grid {
			grid-template-columns: 1fr;
		}
	}
</style>