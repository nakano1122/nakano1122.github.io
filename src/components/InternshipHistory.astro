---
interface InternshipExperience {
	company: string;
	role: string;
	duration: string;
	period: string;
	department: string;
	description: string[];
	technologies: string[];
	achievements?: string[];
	companyLogo?: string;
	status: 'completed' | 'ongoing' | 'upcoming';
}

const internships: InternshipExperience[] = [
	{
		company: "株式会社メルカリ",
		role: "Software Engineer Intern",
		duration: "2ヶ月間",
		period: "2023年8月 - 9月",
		department: "Backend Team",
		description: [
			"マイクロサービスアーキテクチャでの新機能開発を担当",
			"Go言語を用いたRESTful APIの設計・実装",
			"Kubernetesクラスターでの本番環境デプロイを経験",
			"メンターとのペアプログラミングを通じたコードレビュー文化の学習"
		],
		technologies: ["Go", "Kubernetes", "Docker", "gRPC", "PostgreSQL", "Redis"],
		achievements: [
			"新機能のAPIレスポンス時間を30%改善",
			"テストカバレッジを85%まで向上",
			"チーム内でのベストプラクティス共有"
		],
		status: "completed"
	},
	{
		company: "株式会社サイバーエージェント",
		role: "Machine Learning Engineer Intern",
		duration: "1ヶ月間",
		period: "2024年2月 - 3月",
		department: "AI技術部",
		description: [
			"広告配信最適化のための機械学習モデル開発",
			"Python + TensorFlowでの深層学習モデル実装",
			"A/Bテストによる効果検証とデータ分析",
			"MLOpsパイプラインの構築と運用"
		],
		technologies: ["Python", "TensorFlow", "Kubernetes", "BigQuery", "Airflow", "Docker"],
		achievements: [
			"CTR予測精度を12%向上",
			"モデル推論速度を40%高速化",
			"自動化パイプラインの構築で工数50%削減"
		],
		status: "completed"
	},
	{
		company: "LINE株式会社",
		role: "Frontend Engineer Intern",
		duration: "2ヶ月間",
		period: "2024年8月 - 9月",
		department: "LINE Platform Development Team",
		description: [
			"LINEミニアプリのフロントエンド開発を担当",
			"React + TypeScriptでのユーザーインターフェース実装",
			"パフォーマンス最適化とUX改善施策の実施",
			"アクセシビリティ対応とクロスブラウザ互換性の確保"
		],
		technologies: ["React", "TypeScript", "Next.js", "Styled Components", "Jest", "Storybook"],
		achievements: [
			"ページロード時間を45%短縮",
			"ユーザビリティテストでの満足度95%達成",
			"コンポーネントライブラリの標準化"
		],
		status: "upcoming"
	}
];

const getStatusColor = (status: string) => {
	switch (status) {
		case 'completed': return 'var(--color-success)';
		case 'ongoing': return 'var(--color-warning)';
		case 'upcoming': return 'var(--color-accent)';
		default: return 'var(--color-text-light)';
	}
};

const getStatusLabel = (status: string) => {
	switch (status) {
		case 'completed': return '完了';
		case 'ongoing': return '進行中';
		case 'upcoming': return '予定';
		default: return status;
	}
};

const getStatusIcon = (status: string) => {
	switch (status) {
		case 'completed': return '✅';
		case 'ongoing': return '🔄';
		case 'upcoming': return '📅';
		default: return '💼';
	}
};
---

<section class="internship-history section">
	<div class="container">
		<h2 class="section-title">Internship Experience</h2>
		
		<div class="internship-intro animate-slide-up">
			<div class="intro-card">
				<div class="intro-icon">💼</div>
				<div class="intro-content">
					<h3 class="intro-title">インターンシップ経験</h3>
					<p class="intro-description">
						多様な技術領域での実務経験を通じて、エンジニアリングスキルとビジネス理解を深めています。
						バックエンド、機械学習、フロントエンドの各分野で実践的な開発経験を積んでいます。
					</p>
				</div>
			</div>
		</div>

		<div class="internships-timeline">
			{internships.map((internship, index) => (
				<div class="internship-item animate-slide-up" style={`animation-delay: ${index * 0.2}s`}>
					<div class="timeline-connector">
						<div class="timeline-dot" style={`background-color: ${getStatusColor(internship.status)}`}></div>
						{index < internships.length - 1 && <div class="timeline-line"></div>}
					</div>
					
					<div class="internship-card">
						<div class="internship-header">
							<div class="company-info">
								<h3 class="company-name text-gradient">{internship.company}</h3>
								<p class="role-title">{internship.role}</p>
								<div class="department">{internship.department}</div>
							</div>
							<div class="internship-meta">
								<div class="status-badge" style={`background-color: ${getStatusColor(internship.status)}20; color: ${getStatusColor(internship.status)}`}>
									<span class="status-icon">{getStatusIcon(internship.status)}</span>
									{getStatusLabel(internship.status)}
								</div>
								<div class="duration-info">
									<div class="period">{internship.period}</div>
									<div class="duration">{internship.duration}</div>
								</div>
							</div>
						</div>

						<div class="internship-content">
							<div class="description-section">
								<h4 class="content-title">業務内容</h4>
								<ul class="description-list">
									{internship.description.map((item) => (
										<li class="description-item">{item}</li>
									))}
								</ul>
							</div>

							<div class="technologies-section">
								<h4 class="content-title">使用技術</h4>
								<div class="tech-tags">
									{internship.technologies.map((tech) => (
										<span class="tech-tag">{tech}</span>
									))}
								</div>
							</div>

							{internship.achievements && (
								<div class="achievements-section">
									<h4 class="content-title">主な成果</h4>
									<div class="achievements-grid">
										{internship.achievements.map((achievement) => (
											<div class="achievement-item">
												<span class="achievement-icon">🎯</span>
												<span class="achievement-text">{achievement}</span>
											</div>
										))}
									</div>
								</div>
							)}
						</div>
					</div>
				</div>
			))}
		</div>

		<div class="internship-summary animate-slide-up">
			<div class="summary-grid">
				<div class="summary-item">
					<div class="summary-icon">🏢</div>
					<div class="summary-content">
						<div class="summary-number">3</div>
						<div class="summary-label">インターン先企業数</div>
					</div>
				</div>
				<div class="summary-item">
					<div class="summary-icon">⏱️</div>
					<div class="summary-content">
						<div class="summary-number">5</div>
						<div class="summary-label">総実務経験月数</div>
					</div>
				</div>
				<div class="summary-item">
					<div class="summary-icon">🔧</div>
					<div class="summary-content">
						<div class="summary-number">15+</div>
						<div class="summary-label">習得技術数</div>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.internship-history {
		background: var(--color-bg);
	}

	.internship-intro {
		margin-bottom: var(--spacing-3xl);
	}

	.intro-card {
		background: var(--color-bg-card);
		border-radius: var(--radius-xl);
		padding: var(--spacing-2xl);
		box-shadow: var(--shadow-md);
		border: 1px solid var(--color-border);
		display: flex;
		align-items: center;
		gap: var(--spacing-xl);
		max-width: 800px;
		margin: 0 auto;
	}

	.intro-icon {
		font-size: 3rem;
		padding: var(--spacing-lg);
		background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
		border-radius: var(--radius-lg);
		display: flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
		box-shadow: var(--shadow-lg);
	}

	.intro-content {
		flex: 1;
	}

	.intro-title {
		font-size: 1.5rem;
		font-weight: 600;
		margin-bottom: var(--spacing-sm);
		color: var(--color-text);
	}

	.intro-description {
		color: var(--color-text-light);
		line-height: 1.7;
	}

	.internships-timeline {
		position: relative;
		max-width: 1000px;
		margin: 0 auto;
	}

	.internship-item {
		display: flex;
		margin-bottom: var(--spacing-3xl);
		animation: slideUp 0.6s ease-out both;
	}

	.internship-item:last-child {
		margin-bottom: 0;
	}

	.timeline-connector {
		display: flex;
		flex-direction: column;
		align-items: center;
		margin-right: var(--spacing-xl);
		flex-shrink: 0;
	}

	.timeline-dot {
		width: 20px;
		height: 20px;
		border-radius: 50%;
		border: 4px solid var(--color-bg-card);
		box-shadow: var(--shadow-md);
		z-index: 2;
	}

	.timeline-line {
		width: 2px;
		height: 100%;
		background: var(--color-border);
		margin-top: var(--spacing-sm);
		min-height: 80px;
	}

	.internship-card {
		background: var(--color-bg-card);
		border-radius: var(--radius-xl);
		padding: var(--spacing-2xl);
		box-shadow: var(--shadow-lg);
		border: 1px solid var(--color-border);
		flex: 1;
		transition: transform 0.3s ease, box-shadow 0.3s ease;
		position: relative;
		overflow: hidden;
	}

	.internship-card:hover {
		transform: translateY(-4px);
		box-shadow: var(--shadow-xl);
	}

	.internship-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 4px;
		background: linear-gradient(90deg, var(--color-primary) 0%, var(--color-secondary) 100%);
	}

	.internship-header {
		display: flex;
		justify-content: between;
		gap: var(--spacing-xl);
		margin-bottom: var(--spacing-2xl);
		padding-bottom: var(--spacing-lg);
		border-bottom: 1px solid var(--color-border);
	}

	.company-info {
		flex: 1;
	}

	.company-name {
		font-size: 1.75rem;
		font-weight: 700;
		margin-bottom: var(--spacing-xs);
	}

	.role-title {
		font-size: 1.25rem;
		color: var(--color-text-light);
		font-weight: 500;
		margin-bottom: var(--spacing-xs);
	}

	.department {
		color: var(--color-text-lighter);
		padding: var(--spacing-xs) var(--spacing-sm);
		background: var(--color-bg-secondary);
		border-radius: var(--radius-sm);
		display: inline-block;
		font-size: 0.875rem;
	}

	.internship-meta {
		display: flex;
		flex-direction: column;
		align-items: flex-end;
		gap: var(--spacing-sm);
	}

	.status-badge {
		display: flex;
		align-items: center;
		gap: var(--spacing-xs);
		padding: var(--spacing-xs) var(--spacing-sm);
		border-radius: var(--radius-md);
		font-size: 0.875rem;
		font-weight: 600;
		white-space: nowrap;
	}

	.status-icon {
		font-size: 1rem;
	}

	.duration-info {
		text-align: right;
	}

	.period {
		font-size: 0.875rem;
		color: var(--color-text-light);
		font-weight: 500;
	}

	.duration {
		font-size: 0.75rem;
		color: var(--color-text-lighter);
	}

	.internship-content {
		display: flex;
		flex-direction: column;
		gap: var(--spacing-xl);
	}

	.content-title {
		font-size: 1.125rem;
		font-weight: 600;
		margin-bottom: var(--spacing-md);
		color: var(--color-text);
		display: flex;
		align-items: center;
		gap: var(--spacing-sm);
	}

	.content-title::before {
		content: '';
		width: 4px;
		height: 20px;
		background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
		border-radius: 2px;
	}

	.description-list {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: var(--spacing-sm);
	}

	.description-item {
		padding: var(--spacing-sm) var(--spacing-md);
		background: var(--color-bg-secondary);
		border-radius: var(--radius-md);
		color: var(--color-text-light);
		line-height: 1.6;
		position: relative;
		padding-left: var(--spacing-xl);
	}

	.description-item::before {
		content: '▸';
		position: absolute;
		left: var(--spacing-md);
		color: var(--color-primary);
		font-weight: 600;
	}

	.tech-tags {
		display: flex;
		flex-wrap: wrap;
		gap: var(--spacing-xs);
	}

	.tech-tag {
		padding: var(--spacing-xs) var(--spacing-sm);
		background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
		color: white;
		border-radius: var(--radius-sm);
		font-size: 0.875rem;
		font-weight: 500;
		transition: transform 0.2s ease;
	}

	.tech-tag:hover {
		transform: scale(1.05);
	}

	.achievements-grid {
		display: flex;
		flex-direction: column;
		gap: var(--spacing-sm);
	}

	.achievement-item {
		display: flex;
		align-items: center;
		gap: var(--spacing-sm);
		padding: var(--spacing-sm) var(--spacing-md);
		background: linear-gradient(135deg, var(--color-success) 0%, var(--color-accent) 100%);
		background-clip: padding-box;
		border-radius: var(--radius-md);
		color: white;
		font-weight: 500;
		font-size: 0.875rem;
	}

	.achievement-icon {
		font-size: 1rem;
		flex-shrink: 0;
	}

	.internship-summary {
		margin-top: var(--spacing-3xl);
	}

	.summary-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
		gap: var(--spacing-lg);
		max-width: 700px;
		margin: 0 auto;
	}

	.summary-item {
		background: var(--color-bg-card);
		border-radius: var(--radius-lg);
		padding: var(--spacing-xl);
		box-shadow: var(--shadow-md);
		border: 1px solid var(--color-border);
		display: flex;
		align-items: center;
		gap: var(--spacing-lg);
		transition: transform 0.3s ease;
	}

	.summary-item:hover {
		transform: translateY(-2px);
	}

	.summary-icon {
		font-size: 2rem;
		padding: var(--spacing-md);
		background: var(--color-bg-secondary);
		border-radius: var(--radius-md);
		flex-shrink: 0;
	}

	.summary-content {
		flex: 1;
	}

	.summary-number {
		font-size: 2rem;
		font-weight: 700;
		background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		margin-bottom: var(--spacing-xs);
	}

	.summary-label {
		color: var(--color-text-light);
		font-weight: 500;
		font-size: 0.875rem;
	}

	@media (max-width: 768px) {
		.intro-card {
			flex-direction: column;
			text-align: center;
			padding: var(--spacing-xl);
		}

		.intro-icon {
			font-size: 2.5rem;
			padding: var(--spacing-md);
		}

		.timeline-connector {
			margin-right: var(--spacing-md);
		}

		.internship-card {
			padding: var(--spacing-xl);
		}

		.internship-header {
			flex-direction: column;
			gap: var(--spacing-lg);
		}

		.internship-meta {
			align-items: flex-start;
		}

		.duration-info {
			text-align: left;
		}

		.summary-grid {
			grid-template-columns: 1fr;
		}

		.summary-item {
			padding: var(--spacing-lg);
		}
	}
</style>